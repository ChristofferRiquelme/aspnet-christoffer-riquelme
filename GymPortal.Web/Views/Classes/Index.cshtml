@using GymPortal.Domain

@model List<GymClass>

<div class="max-w-4xl mx-auto p-6">
    <h1 class="text-3xl font-bold mb-6">Pass</h1>

    <div class="grid gap-4">
        @foreach (var item in Model)
        {
            <partial name="_ClassCard" model="item" />
        }
    </div>
</div>